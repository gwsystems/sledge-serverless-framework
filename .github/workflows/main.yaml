# CI workflow
name: sledge
on: [push, pull_request]

runs:
  using: "docker"
  image: "Dockerfile.x86_64"

# job control
jobs:
  build:
    steps:
      - name: Install
        run: make clean install
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Clang Format
        run: ./format.sh -d
